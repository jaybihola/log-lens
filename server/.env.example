# Copy to .env (git-ignored) for a default remote-query credential — the app's
# Credentials modal can also set this at runtime, which takes precedence.
ES_USERNAME=you@example.com
ES_PASSWORD=your-password
